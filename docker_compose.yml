services:
  frontend:
    container_name: frontend
    build:
      context: ./frontend
    ports:
      - "80:80"

  backend:
    container_name: backend
    env_file: 
      - .env 
    build:
      context: ./backend